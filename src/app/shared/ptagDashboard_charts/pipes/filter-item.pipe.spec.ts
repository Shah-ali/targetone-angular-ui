import { FilterItemsPipe} from './filter-item.pipe';

describe('FilterItemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
